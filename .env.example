# Required: Secret key for HMAC-SHA256 anti-cheat checksums.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Must be set in production (Vercel) environment variables — never commit the real value.
CHECKSUM_SECRET=